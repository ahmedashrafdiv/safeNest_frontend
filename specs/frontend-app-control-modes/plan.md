# Plan: Frontend App-Control Modes

Extend Parent digital-rule models and installed-app UI with allowlist/blocklist mode selection. Extend Child rule synchronization, local preference storage, pure policy decisions, AccessibilityService enforcement, and blocked-screen messaging. Add deterministic Child tests, build both APKs, run Backend regression tests, and leave live paired-device verification explicitly tracked.
