# Plan: Parent Startup Inbox

1. Verify the existing alert and child-access-request API contracts.
2. Add Parent Retrofit models and repository methods for loading pending data and decisions.
3. Implement the RTL Layngo inbox screen and request/notification cards.
4. Route authenticated startup and post-login through the inbox with a safe Home fallback.
5. Build the Parent app and run Backend regression tests.
6. Review the feature, add focused tests where the project setup supports them, and keep live-device verification separate from local build evidence.
