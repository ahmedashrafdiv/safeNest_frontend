# Plan: Frontend Installed-App Management

1. Confirm the Parent Retrofit models and fragment use the shared installed-app contract.
2. Verify inventory loading, refresh, empty, error, and retry behavior.
3. Verify block/unblock and per-app time-limit payloads use package names.
4. Add focused model, payload, and UI-state tests where supported by the Android test setup.
5. Build Parent and Child debug APKs and run configured tests.
6. Review and fix only findings attributable to this feature.
